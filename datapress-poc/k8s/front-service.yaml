apiVersion: v1
kind: Service
metadata:
  name: datapress-front
  namespace: datapress-recette
spec:
  type: NodePort
  selector:
    app: datapress-front
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080  # Optionnel, sinon laisser Kubernetes choisir
